name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            script: build:linux
            artifact: omniplayr-linux
          - os: windows-latest
            script: build:win
            artifact: omniplayr-win.exe
          - os: macos-latest
            script: build:mac-arm64
            artifact: omniplayr-macos-arm64
          - os: macos-13
            script: build:mac-x64
            artifact: omniplayr-macos-x64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci

      - run: npm run ${{ matrix.script }}

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - run: npm ci

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Upload binaries to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/*

      - name: Update Homebrew formula
        env:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          VERSION=${GITHUB_REF_NAME#v}

          SHA_ARM=$(shasum -a 256 dist/omniplayr-macos-arm64 | awk '{print $1}')
          SHA_X64=$(shasum -a 256 dist/omniplayr-macos-x64 | awk '{print $1}')
          SHA_LINUX=$(shasum -a 256 dist/omniplayr-linux | awk '{print $1}')

          git clone https://x-access-token:${HOMEBREW_TAP_TOKEN}@github.com/OmniPlayr/homebrew-tap.git
          cd homebrew-tap

          sed -i "s/version \".*\"/version \"$VERSION\"/" Formula/omniplayr.rb
          sed -i "s|releases/download/v[^/]*/omniplayr-macos-arm64|releases/download/v$VERSION/omniplayr-macos-arm64|" Formula/omniplayr.rb
          sed -i "s|releases/download/v[^/]*/omniplayr-macos-x64|releases/download/v$VERSION/omniplayr-macos-x64|" Formula/omniplayr.rb
          sed -i "s|releases/download/v[^/]*/omniplayr-linux|releases/download/v$VERSION/omniplayr-linux|" Formula/omniplayr.rb
          sed -i "/omniplayr-macos-arm64/{n;s/sha256 \".*\"/sha256 \"$SHA_ARM\"/}" Formula/omniplayr.rb
          sed -i "/omniplayr-macos-x64/{n;s/sha256 \".*\"/sha256 \"$SHA_X64\"/}" Formula/omniplayr.rb
          sed -i "/omniplayr-linux/{n;s/sha256 \".*\"/sha256 \"$SHA_LINUX\"/}" Formula/omniplayr.rb

          git config user.email "info@wokki20.nl"
          git config user.name "OmniPlayr CI"
          git add Formula/omniplayr.rb
          git commit -m "Update omniplayr to v$VERSION"
          git push

      - name: Update Scoop manifest
        env:
          SCOOP_BUCKET_TOKEN: ${{ secrets.SCOOP_BUCKET_TOKEN }}
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          SHA_WIN=$(shasum -a 256 dist/omniplayr-win.exe | awk '{print $1}')

          git clone https://x-access-token:${SCOOP_BUCKET_TOKEN}@github.com/OmniPlayr/scoop-bucket.git
          cd scoop-bucket

          cat > omniplayr.json << EOF
          {
            "version": "$VERSION",
            "description": "OmniPlayr CLI",
            "homepage": "https://github.com/OmniPlayr/OmniPlayr-CLI",
            "license": "MIT",
            "architecture": {
              "64bit": {
                "url": "https://github.com/OmniPlayr/OmniPlayr-CLI/releases/download/v$VERSION/omniplayr-win.exe",
                "hash": "$SHA_WIN"
              }
            },
            "bin": "omniplayr-win.exe"
          }
          EOF

          git config user.email "info@wokki20.nl"
          git config user.name "OmniPlayr CI"
          git add omniplayr.json
          git commit -m "Update omniplayr to v$VERSION"
          git push